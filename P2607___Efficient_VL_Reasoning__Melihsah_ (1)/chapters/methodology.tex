\chapter{Methodology: Data, Evaluation Protocol and Architectures}
\section{Methodological framing and design principles}
\label{sec:ch3-framing}
This chapter defines the methodology used to study closed-set visual question answering on GQA \citep{hudson2019gqa}. The central problem is whether lightweight trainable heads can make effective use of frozen multimodal representations, while keeping development-set accuracy and measured efficiency, understood here as warm-serial latency and trainable parameter count, in view. Within the global pathway, fixed representations come from the separate image and text towers of a frozen CLIP checkpoint.
% CITE-GAP clip_foundation
This broader use of frozen pretrained components is established in vision--language research \citep{tsimpoukelli2021frozen}, but here it is treated as a controlled design choice rather than a claim about any one architecture.

Four principles govern the experiments. First, representation encoders remain frozen, so comparisons concern the trainable interface or head. Second, matched controls are used wherever an architectural or pretraining choice is isolated. Third, models are replicated across predefined seeds rather than interpreted from a single run. Finally, development evidence is kept separate from future confirmatory evaluation: it supports model development and analysis, but not a held-out generalisation claim.

The formal research questions are to be stated in Chapter 1.
% XREF-PENDING: Chapter 1 RQ definitions
As an organising map, RQ1 concerns global heads and representation or answer-space choices; RQ2 concerns token-level reasoning under data scaling; and RQ3 concerns frozen small-language-model interfaces and their bounded efficiency evidence. Chapters 4--6 address these questions in turn, while Chapter 7 reports the efficiency analysis.

\section{Task definition: closed-set VQA on GQA}
\label{sec:ch3-task}
The experimental task uses the balanced release of GQA, a benchmark designed around questions grounded in image content and compositional scene structure \citep{hudson2019gqa}. Each example supplies an image and a natural-language question, and the system maps that pair to one answer from a fixed vocabulary. This is the primary task throughout the dissertation: discriminative closed-set VQA, rather than unrestricted language generation. Closed-vocabulary classification has a long-standing role in VQA evaluation \citep{antol2015vqa,jabri2016revisiting}; in this project, the answer vocabulary is constructed from the training pool only, so development images cannot determine its support.

Two later experiment families use bounded generation only as an evaluation readout. In the answer-side language-model family (E8B, described in Section 3.7), the R2 readout decodes greedily under a prefix trie restricted to the closed answer vocabulary, while the R3 readout removes that restriction but stays greedy and stops at a fixed token cap. The compact vision--language models used for context (E9, defined in Section 3.7) are likewise evaluated with a pinned greedy readout. These exceptions test how an already defined representation or readout produces an answer; they do not extend the task to open-ended generation. Sampling, temperature search and prompt search are excluded.

Keeping this distinction explicit prevents a change in output mechanism from being mistaken for a change in the scientific task. The same boundary applies to every classifier and readout family: the image--question input is fixed, answer production is bounded, and every trainable component is fitted to a fixed answer set defined for that experiment. The training objective differs by family: the classifier heads and the language-model-free readout use a cross-entropy loss over the closed answer vocabulary, whereas the answer-side language-model readouts are trained with a teacher-forced token-level loss on the answer tokens (each answer token is predicted from the preceding gold tokens), with decoding bounded at evaluation as described above. The scoring rules and evaluation denominators are defined in Section 3.5.

\section{Data partitioning and nested training scales}
\label{sec:ch3-data}
The GQA data \citep{hudson2019gqa} were partitioned at image level so that questions associated with one image cannot be split between development and the training pool. As summarised in Table~\ref{tab:ch3-partition}, the complete development partition contains 10,004 raw questions on 777 images. Applying the fixed-vocabulary filter retains 7,714 questions represented across 768 images. The two image counts therefore describe different scopes: 777 is the complete partition, whereas 768 counts images represented by retained in-vocabulary rows.

\begin{table}[htbp]
\centering
\caption{Data partitions and nested training scales. Image counts distinguish complete-partition images from images represented by retained rows; question counts distinguish eligible pool rows from selected training-prefix rows.}
\label{tab:ch3-partition}
\begingroup
\footnotesize
\setlength{\tabcolsep}{2.5pt}
\renewenvironment{longtable}[1]
  {\begin{tabular}{|p{0.15\textwidth}|p{0.13\textwidth}|p{0.11\textwidth}|p{0.27\textwidth}|p{0.26\textwidth}|}}
  {\end{tabular}}
\let\endhead\relax
\input{tables/ch3_partition_stats.tex}
\endgroup
\end{table}

The remaining training partition contains 71,363 images, while the vocabulary rule makes 724,074 questions eligible for selection. Three training manifests take strict row-for-row prefixes of this shared question order: 40k is contained in 100k, which is contained in 250k. Their represented-image counts are 27,622, 46,158 and 61,859 respectively. Figure~\ref{fig:ch3-partition} shows this nesting together with the image-disjoint boundaries.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{ch3/FIG-3-A.pdf}
    \caption{Image-disjoint data regions and row-for-row nesting of the training-question prefixes. The clean-test region is shown only at structural level; its targets remain embargoed.}
    \label{fig:ch3-partition}
\end{figure}

This is a question-level scaling design. Moving to a larger prefix adds images that were not represented earlier, but can also add further questions for images already present. The manifests cannot separate these two effects, so Chapter 8 treats this as a design limitation rather than attributing scaling effects to image count alone.

The clean-test region is image-disjoint from both development and training. Only its structural counts are reported: 8,013 questions on 972 images. Targets remain embargoed until the model list is frozen, as set out in Section 3.12. No clean-test result exists yet, and none has informed the methods or development choices reported here.

\section{Answer-space construction: top-100 and top-1000}
\label{sec:ch3-answer-space}
The main answer space contains the 100 most frequent raw answers in the training pool. Answers are ordered by decreasing count, with alphabetical order used to break ties. Development examples do not contribute to this construction. A second answer space extends the same rule to the 1,000 most frequent training-pool answers. Its first 100 entries are identical to the smaller vocabulary, including their indices, so the extension does not silently change the meaning of an existing class.

These choices define which development questions can be scored within each closed vocabulary. The top-100 vocabulary covers 7,714 of the 10,004 raw development rows, giving a coverage of 0.7711. The top-1000 extension covers 9,823 rows, or 0.9819. These figures describe answer-space coverage rather than model performance. They show the evaluation support for each experiment, not how accurately any system answers the retained questions.

Pinned protocol artefacts give each experiment its answer-to-index mapping. The shared prefix extends rather than revises the top-100 labels, keeping answer-space design separate from representation and architecture choices. Section~\ref{sec:ch3-scoring} defines how the vocabularies lead to different evaluation denominators.

\section{Scoring metrics and evaluation denominators}
\label{sec:ch3-scoring}
The primary strict metric is raw exact match: a prediction is correct only when its answer string exactly matches the single canonical gold answer. The project also uses one pinned normalised exact-match metric. It applies a fixed, single-gold translation of the standard VQA answer normalisation before making the same exact comparison. This retains the normalisation lineage of VQA evaluation \citep{antol2015vqa}, but it is not the official VQA soft or consensus accuracy, which averages agreement over several human answers.

Within the top-100 vocabulary, normalisation changes none of the answer strings and creates no collisions. Raw and normalised correctness therefore coincide for these classifiers. This is a property of the pinned answer set, not a general claim about normalised scoring. Semantic similarity, synonym matching, embedding-based scoring and language-model judgement are excluded from the primary metrics.

Table~\ref{tab:ch3-answer-spaces} separates the three reporting denominators: 7,714 top-100 rows, 9,823 top-1000 rows and all 10,004 raw development rows. They are never mixed silently. Across answer spaces, coverage-adjusted raw-distribution accuracy is the in-vocabulary accuracy multiplied by coverage. Project scores are not compared with published GQA scores \citep{hudson2019gqa}, because the answer spaces, scoring rule and denominators differ.

\begin{table}[htbp]
\centering
\caption{Answer-space coverage and evaluation roles. Coverage values are design properties, not model-accuracy results.}
\label{tab:ch3-answer-spaces}
\begingroup
\scriptsize
\setlength{\tabcolsep}{2pt}
\renewenvironment{longtable}[1]
  {\begin{tabular}{|p{0.12\textwidth}|p{0.09\textwidth}|p{0.13\textwidth}|p{0.08\textwidth}|p{0.23\textwidth}|p{0.25\textwidth}|}}
  {\end{tabular}}
\let\endhead\relax
\input{tables/ch3_answer_spaces.tex}
\endgroup
\end{table}

\section{Frozen representations and feature extraction}
\label{sec:ch3-representations}
The global pathway uses one frozen OpenCLIP ViT-B-32 checkpoint, \texttt{laion2b\_s34b\_b79k}. Its separate image and text towers produce L2-normalised 512-dimensional global vectors. The towers belong to one jointly pretrained checkpoint, so their outputs occupy an aligned pretrained space; this does not mean that the same encoder processes both modalities.
% CITE-GAP clip_foundation
Freezing the representation model keeps training focused on the lightweight interface, following the broader frozen-model setting described by \citet{tsimpoukelli2021frozen}.

\begingroup
\setlength{\emergencystretch}{2em}
The SigLIP comparison replaces only this global pathway with the frozen \texttt{ViT-B-16-}\allowbreak\texttt{SigLIP} checkpoint using \texttt{webli} weights. It supplies 768-dimensional inputs to the same head recipe. The token-level stores used by the reasoner remain CLIP features, so this encoder swap is not carried into the token pathway.\par
\endgroup
% CITE-GAP siglip

The visual token store contains 50 vectors per image: one CLS token and 49 patch tokens, all with width 512. Question tokens are stored at their packed sequence lengths with the same width. These CLIP stores provide a jointly pretrained token space for the latent reasoner. The question-side small-language-model interface instead uses frozen SmolLM2-135M hidden states from pinned revision \texttt{93efa2f0...}. A trainable \texttt{Linear(576, 512)} layer connects those states to the reasoner. This common width is a learned interface and is not evidence that CLIP and SmolLM2 naturally share a pretrained semantic space. The 360M question-side arms were authorised but not run.
% CITE-GAP smollm2

For the answer-side E8B family, canonical evaluation is performed in FP32. This precision rule applies only to E8B evaluation; it does not redefine training precision elsewhere.

\section{Architecture families}
\label{sec:ch3-architectures}
Figure~\ref{fig:ch3-families} separates frozen components, trainable interfaces and the contextual class. Table~\ref{tab:ch3-architectures} gives exact trainable-parameter counts, which measure capacity rather than latency.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{ch3/FIG-3-B.pdf}
    \caption[Architecture-family overview.]{Architecture families and their frozen, trainable or evaluation-only contextual roles.}
    \label{fig:ch3-families}
\end{figure}

\subsection{Global-embedding heads}
Global heads share an \texttt{MLPHead}, normally at hidden width 512. Its layers are linear, ReLU, dropout and linear. The \texttt{question\_only} and \texttt{image\_only} controls use one vector. \texttt{concat} uses $[i;q]$; \texttt{fusion} uses the two globals, their product and absolute difference in the order $[i;q;i\odot q;|i-q|]$.

Capacity controls use width 979 for \texttt{concat\_wide} and 268 for \texttt{fusion\_narrow}. Product-only and difference-only controls use width 351 (\texttt{product\_576k}, \texttt{difference\_576k}) or the natural width 512 (the separately executed \texttt{product\_natural}, \texttt{difference\_natural}). The \texttt{direct\_linear} reference is \texttt{Linear(1024, 100)}.

E2 applies the same head family to 768-dimensional inputs and is not parameter-matched to the 512-dimensional heads; E3 changes the output to 1,000 classes. Thus v2\_03 varies capacity with features fixed, v2\_04 varies features at controlled capacity, and E2 varies the frozen representation with the head recipe fixed.

\subsection{Token-level latent reasoner}
The reasoner uses 32 learned latent queries \citep{jaegle2021perceiver}. Four pre-layer-normalised blocks apply question cross-attention, image cross-attention, latent self-attention and a fourfold GELU feed-forward network, in that order. Cross-attention reads an input sequence; self-attention connects the latents. Attention has 8 heads at width 512 and no input projections.

The final latents are mean-pooled and layer-normalised. The readout is \texttt{Linear(512,100)}. The reasoner has 21,099,620 trainable parameters. Figure~\ref{fig:ch3-reasoner} shows this order and the frozen inputs. Mean-patch concatenation and \texttt{direct\_linear} are references.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{ch3/FIG-3-C.pdf}
    \caption[Token-level latent reasoner.]{Token-level reasoner, showing the ordered block operations and the absence of input projections.}
    \label{fig:ch3-reasoner}
\end{figure}

\subsection{Question-side small-language-model interface}
The question-side interface supplies three inputs to one trainable reasoner and classifier. A0p applies \texttt{Linear(512,\allowbreak 512)} to frozen CLIP tokens. A1/A1r apply \texttt{Linear(576,\allowbreak 512)} to frozen pretrained/matched-random SmolLM2-135M states. The encoders stay frozen; a learned common width does not create a shared pretrained space.

\subsection{Answer-side small-language-model interface}
The answer-side path projects 32 latents into the frozen model's embedding width. B1 is an attention-pool classifier. B2/B3 are matched-random/pretrained 135M readouts; B4r/B4 are the 360M pair. Counts are shared within each size, not between initialisations. R1 ranks candidate answers by likelihood, while R2 and R3 are the trie-constrained greedy and bounded free-greedy readouts from Section~\ref{sec:ch3-task}. Figure~\ref{fig:ch3-slm-interface} relates both interfaces.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{ch3/FIG-3-D.pdf}
    \caption[Small-language-model interfaces.]{Question- and answer-side frozen small-language-model interfaces, showing trainable projections and matched-random controls.}
    \label{fig:ch3-slm-interface}
\end{figure}

\subsection{Compact vision--language models for context}
Frozen SmolVLM-256M-Instruct and SmolVLM-500M-Instruct are contextual only. They have zero trainable parameters and are not matched with the trained systems. The pinned Instruct checkpoints do not define a base-versus-Instruct contrast.
% CITE-GAP smolvlm

\subsection{Matched random controls}
A matched-random control keeps its partner's architecture and tokenizer. Pinned-seed random weights are frozen and not tuned separately. Within-size question-side pairs isolate the input representation; answer-side pairs isolate readout initialisation. Effects are reported outside this chapter.

\begin{table}[p]
\centering
\caption[Trainable-parameter accounting by architecture family.]{Trainable parameters by architecture. Loaded and resident-frozen totals are omitted. The shared natural-width row covers separately executed \texttt{product\_natural} and \texttt{difference\_natural} systems, with 838,244 parameters each.}
\label{tab:ch3-architectures}
\begingroup
\scriptsize
\setlength{\tabcolsep}{2.2pt}
\renewenvironment{longtable}[1]
  {\begin{tabular}{|p{0.17\textwidth}|p{0.25\textwidth}|p{0.35\textwidth}|p{0.14\textwidth}|}}
  {\end{tabular}}
\let\endhead\relax
\input{tables/ch3_architecture_params.tex}
\endgroup
\end{table}

\section{Training, checkpoints and model selection}
% TODO dissertation-01-ch3-prep-s3: internal budget 250 words; structural scaffold only.

\section{Statistical methodology}
% TODO dissertation-01-ch3-prep-s4: internal budget 300 words; structural scaffold only.

\section{Visual-reliance intervention design}
% TODO dissertation-01-ch3-prep-s4: internal budget 150 words; structural scaffold only.

\section{Efficiency measurement protocol}
% TODO dissertation-01-ch3-prep-s4: internal budget 250 words; structural scaffold only.

\section{Reproducibility, provenance and clean-test governance}
% TODO dissertation-01-ch3-prep-s5: internal budget 250 words; PRE-F2: final wording depends on F2 state at submission; structural scaffold only.
