"""Source package for the project.

Holds the reusable code imported by the numbered stage scripts:

  - data.py: dataset and dataloaders over the cached embedding vectors,
  - models.py: the MLP head, the three baselines and the proposed fusion model,
  - utils.py: seeding, device selection, parameter counting, timing and saving
    results.
"""
